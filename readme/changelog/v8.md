# Changelog — Ver 8 (mới nhất): Upload ảnh bìa, sửa lỗi treo khi thêm nhạc, sửa lỗi ảnh cover không hiển thị, hợp nhất Settings Visualizer/Phụ đề thành drawer riêng, Control Center kính mờ kiểu iPhone, bỏ điều khiển từ ngoài app, và nhiều refine UI khác.

> Vòng này trải qua nhiều lượt: bắt đầu từ tính năng chỉnh sửa metadata bài
> hát (modal "Sửa thông tin" có thêm tab "Ảnh bìa"), logo wordmark "SAV",
> tách `settings-drawer.js` thành module nhiều file, gom changelog vào thư
> mục riêng — sau đó là loạt REFINE theo phản hồi thực tế: logo viết lại
> đúng kiểu trượt ngang không khung, 2 modal thiết kế lại theo `glass-modal`
> rõ nét hơn, dải dọc icon ở màn Visualizer thay bằng "Control Center" phóng
> ra từ trung tâm với kính mờ trong suốt kiểu iPhone, Settings Visualizer/
> Phụ đề hợp nhất vào drawer riêng (giống About) chỉ giữ lại phần cần dùng
> ngay — và 2 lượt FIX LỖI THẬT: (1) thêm nhạc bị treo hoàn toàn do
> `input[type=file]` chặn `.click()` gọi qua JS trên một số nền tảng, sửa
> bằng `<label>` bọc input thật; (2) ảnh cover không hiển thị do
> `jsmediatags` đôi khi trả MIME rác, sửa bằng validate + fallback ảnh vinyl
> mặc định ở mọi nơi hiển thị cover. Cuối vòng: dừng hẳn nhạc + reset hoàn
> toàn (không còn "pause tạm") khi tab/app bị ẩn, và bỏ hẳn khả năng điều
> khiển play/pause/next/prev từ màn khoá/tai nghe/thông báo hệ thống.

---

## 1. Modal "Sửa thông tin bài hát" có thêm tab "Ảnh bìa"

**Vấn đề gốc:** trình sửa thông tin (mở từ menu 3 chấm → "Sửa thông tin")
trước nay chỉ có 3 ô nhập text (tên bài/nghệ sĩ/album). Muốn đổi ảnh bìa của
1 bài hát cụ thể không có cách nào làm được trong app — trong khi cơ chế
lưu trữ (`record.cover` trong IndexedDB) và cơ chế xuất tag (`buildTaggedBlob()`
ở `id3-export.js`, ghi frame `APIC` từ `record.cover`) đã có sẵn từ ver 5,
chỉ thiếu đúng 1 chỗ để NGƯỜI DÙNG ghi vào field đó.

**Thiết kế UI — 2 tab trong cùng 1 modal** (đúng yêu cầu: không tách modal
riêng, gộp vào modal sửa thông tin sẵn có dưới dạng tab):

- **Tab "Thông tin"** — giữ nguyên 3 ô nhập cũ, không đổi hành vi.
- **Tab "Ảnh bìa"** (mới) — preview hình vuông 80×80, nút "Chọn ảnh" (input
  file ẩn, label làm nút bấm — cùng pattern với `setting-bg-upload` ở
  Settings) và nút "Xóa ảnh bìa". Validate file bằng `validateImageFile()`
  đã có sẵn từ ver 7 (chỉ nhận PNG/JPG/WEBP), tái dùng 100% logic, không
  viết lại whitelist riêng.
- Chuyển tab thuần bằng JS (`setSongEditTab()`), không có animation phức
  tạp — chỉ ẩn/hiện 2 khối + đổi màu nút tab đang active.

**Luồng dữ liệu — áp dụng ảnh CHỈ khi bấm "Lưu" (không ghi DB ngay lúc chọn
file):**

- Biến tạm `songEditPendingCover` giữ 1 trong 3 trạng thái: `null` (không
  đổi gì), 1 `File` (đặt ảnh mới), hoặc chuỗi `'remove'` (xóa ảnh). Chọn
  ảnh mới hoặc bấm "Xóa ảnh bìa" CHỈ cập nhật preview + biến tạm này —
  giống đúng cách 3 ô text hoạt động (gõ vào input không lưu DB ngay).
- Bấm "Hủy" (`closeSongEditModal()`) reset hẳn biến tạm về `null` và revoke
  object URL preview — không có gì được ghi, dù người dùng đã chọn ảnh
  trước đó trong phiên mở modal này.
- Bấm "Lưu": đọc lại `record` từ IndexedDB, merge `tag` mới (như cũ), và
  THÊM: nếu `pendingCover` là `File` → `record.cover = pendingCover` (File
  là 1 dạng Blob, ghi trực tiếp được, giống cách `jsmediatags` đã gán
  `record.cover` lúc nạp file ban đầu); nếu là `'remove'` → `delete
  record.cover`. Toàn bộ nằm trong `withLoadingShield("Đang lưu thông
  tin...", ...)` — hiện spinner trong lúc ghi IndexedDB (đúng yêu cầu
  "loading shield" lúc xử lý ảnh).
- Sau khi lưu: nếu bài đang sửa CHÍNH LÀ bài đang phát (`key === currentKey`),
  cập nhật luôn `#record-art` (đĩa vinyl xoay ở Bottom Player) +
  `navigator.mediaSession.metadata.artwork` — không cần đổi bài rồi đổi lại
  mới thấy ảnh mới.
- `refreshSongNode(key)` được gọi ngay sau khi lưu — vẽ lại đúng node DOM
  của bài đó trong danh sách (ảnh + tên mới), không cần `renderPlaylistFull()`
  toàn bộ danh sách.

**Xuất tệp:** không cần sửa gì ở `id3-export.js` — `buildTaggedBlob()` đã
đọc `record.cover` để ghi frame `APIC` từ ver 5; ảnh mới lưu qua tab "Ảnh
bìa" tự động được ghi vào file mp3 lúc "Xuất tệp" mà không cần code thêm.

**Bộ nhớ object URL:** preview dùng `URL.createObjectURL()` cho cả ảnh HIỆN
TẠI (lúc mở modal) và ảnh MỚI CHỌN (lúc đổi) — `songEditPendingCoverPreviewUrl`
theo dõi URL "đang sống" để `revokeObjectURL()` đúng lúc (đổi ảnh khác,
hoặc đóng modal bằng Hủy/Lưu), tránh rò bộ nhớ giống nguyên tắc đã áp dụng
cho `currentCoverObjectURL` ở `playSong()`.

## 2. Modal sửa thông tin + modal thông tin chi tiết: thiết kế lại theo theme kính mờ "nét hơn" (`glass-modal`)

**Vấn đề (lần 1):** 2 modal `song-edit-modal` và `song-info-modal` dùng nền
đặc `bg-[#0f172a]` (xanh đen solid), không khớp với theme "glassmorphism"
mà phần còn lại của app đang dùng.

**Vấn đề (refine theo phản hồi):** đổi sang `.glass-panel` có sẵn
(`rgba(15,23,42,0.6)` + `blur(24px)`) cho cảm giác "mờ nhạt" — chữ/control
chưa nổi rõ trên mọi ảnh nền Playlist sáng màu.

**Sửa — thêm class `.glass-modal` mới (`css/styles.css`), dùng RIÊNG cho 2
modal này** (không đổi `.glass-panel` đang dùng ở chỗ khác, tránh ảnh hưởng
lan): nền đậm hơn (`rgba(15,23,42,0.82)` so với `0.6`) để chữ/control luôn
nổi rõ, viền sáng hơn (`rgba(255,255,255,0.12)` so với `0.05`), thêm
`box-shadow` glow nhẹ phía trong + đổ bóng ngoài đậm — card vẫn giữ chất
kính mờ (`backdrop-filter: blur(28px)`, thấy mờ ảnh nền Playlist phía sau)
nhưng "nét" và tách bạch khỏi nền hơn hẳn bản trước. Đồng thời thiết kế lại
toàn bộ nội dung 2 modal cho rõ ràng hơn:

- **Header có icon tròn màu** (bút chì cho modal Sửa, dấu "i" cho modal
  Thông tin) + viền dưới phân tách khỏi phần nội dung — thay cho tiêu đề
  trần đứng riêng lẻ.
- **Tab switcher kiểu "pill"**: 2 nút nằm trong 1 rãnh nền tối
  (`bg-black/30`), tab đang chọn nổi lên nền sáng (`bg-white/10`) + chữ
  trắng + đổ bóng nhẹ, tab chưa chọn chỉ còn chữ xám mờ — rõ ràng hơn hẳn 2
  nút viền màu riêng lẻ ở bản đầu.
- **3 ô nhập (Tên bài/Nghệ sĩ/Album)** có icon trái nhỏ riêng cho từng loại
  dữ liệu (nốt nhạc / người / đĩa nhạc), label viết hoa nhỏ kiểu
  `uppercase tracking-wide` đặt trên, nền ô nhập đậm hơn để nổi trên card.
- **Tab Ảnh bìa**: preview lớn hơn (96px, trước 80px) có viền sáng + bóng,
  2 nút hành động đổi từ chữ trần sang **icon + chữ** rõ ràng hơn, ghi chú
  định dạng chấp nhận đặt trong khung riêng có icon thay vì đoạn text trần.
- **Modal Thông tin chi tiết bài hát**: 6 dòng dữ liệu (tên/nghệ sĩ/album/
  thời lượng/số lần nghe/đã nghe) chuyển từ `<div class="flex
  justify-between">` trần thành **card nhỏ riêng từng dòng** — icon tròn
  màu riêng (mỗi loại dữ liệu 1 màu accent khác nhau: sky/violet/emerald/
  amber/rose/indigo) + label viết hoa nhỏ bên trái + giá trị bên phải, nền
  `bg-black/25` tách dòng khỏi nhau — dễ quét mắt hơn nhiều so với list trần
  6 dòng liên tiếp không có điểm phân biệt. Tái cấu trúc thành hàm dựng
  dòng dùng chung `songInfoRowHtml(iconPath, accentClass, label, value)`
  (`js/playlist/actions.js`) để không lặp template 6 lần.

`playback-error-modal` (modal báo lỗi phát nhạc) GIỮ NGUYÊN nền đặc — modal
đó không thuộc nhóm "thông tin/chỉnh sửa ảnh bài hát" nên không đổi.

## 3. Logo "SAV" góc trái màn Playlist — hover trượt chữ theo chiều ngang

**Vị trí:** đối xứng với cụm icon (Thêm nhạc / Cài đặt / Đổi giao diện) ở
góc phải hàng đầu tiên của màn Playlist — hàng đó đổi từ `justify-end`
thành `justify-between` để chèn logo vào đầu hàng.

**Thiết kế (refine theo phản hồi):** bỏ hẳn khung/nền/viền bo tròn của bản
đầu (`bg-white/5`, `border`, `rounded-full`) — giờ chỉ còn chữ trần
`font-extrabold text-white`, đúng kiểu logo wordmark phẳng (không khung,
không viền) như logo Facebook.

**Hiệu ứng trượt — LUÔN 1 DÒNG NGANG, không xuống dòng, xử lý THUẦN BẰNG
CSS:** mỗi chữ hoa (S/A/V) là 1 `<span>` đứng yên làm "mỏ neo"; ngay sau nó
là 1 `<span>` chứa phần chữ thường còn lại của từ đó ("imple "/"udio
"/"isualizer") với `max-width: 0` + `overflow: hidden` + `white-space: pre`
(giữ dấu cách cuối, không xuống dòng) ở trạng thái nghỉ. Hover vào
`#sav-logo` (`group-hover`) thì `max-width` của 3 `<span>` này giãn ra lần
lượt `4.2em` / `3.6em` / `6em` (đủ rộng hơn nội dung thật, có buffer an
toàn cho sai khác font-rendering) — toàn bộ nối liền nhau trên CÙNG 1 DÒNG
NGANG thành "Simple Audio Visualizer"; bỏ hover thì animate NGƯỢC LẠI co
về "S A V" rời nhau (đúng yêu cầu "thu lại từ từ"). 3 `<span>` có
`transition-delay` tăng dần 0/60/120ms để tạo cảm giác "trượt nối tiếp" từ
trái qua phải thay vì cả 3 từ nở cùng lúc — cùng 1 `transition` cho cả 2
chiều (hover vào/ra) nên animation tự đối xứng, không cần class riêng theo
chiều. Không dùng `width: auto` (không animate được bằng CSS transition
thuần) — đây là lý do chọn `max-width` với 1 giá trị đích đủ lớn hơn nội
dung thật, đúng kỹ thuật "logo HTML5" kinh điển.

## 4. Tìm kiếm theo Album

**Vấn đề:** `matchesSearch()` (ở `playlist/order.js`) trước đây chỉ so khớp
`tag.title` và `tag.artist`, bỏ qua `tag.album` — gõ tên album vào ô tìm
kiếm không ra kết quả nào dù bài hát đó thực sự thuộc album đó.

**Sửa:** thêm 1 dòng so khớp `normalizeSongName(cached.tag.album)` y hệt 2
field còn lại (cùng hàm chuẩn hóa bỏ dấu/hạ thường đã dùng cho title/artist
— không cần logic riêng). Placeholder ô tìm kiếm cũng cập nhật từ "Tìm bài
hát, nghệ sĩ..." thành "Tìm bài hát, nghệ sĩ, album..." để người dùng biết
tính năng này tồn tại.

## 4b. Thêm nhạc — hỗ trợ chọn cả thư mục (`webkitdirectory`)

**Vấn đề:** nút "Thêm nhạc" chỉ mở 1 hộp thoại chọn file rời
(`<input type="file" multiple>`) — muốn nạp nguyên 1 thư mục nhạc phải tự
chọn từng file (hoặc bôi đen tất cả trong hộp thoại, vẫn bất tiện khi nhạc
nằm rải trong nhiều thư mục con).

**Sửa — thêm 1 input riêng cho thư mục, KHÔNG đổi input file rời sẵn có:**
`<input type="file" id="audio-upload-folder" webkitdirectory directory
multiple>` (`index.html`) — thuộc tính `webkitdirectory` bắt trình duyệt mở
hộp thoại CHỌN THƯ MỤC thay vì chọn file; kết quả trả về vẫn là 1 `FileList`
bình thường (gồm toàn bộ file trong thư mục VÀ thư mục con), không khác gì
input file rời về mặt dữ liệu.

Nút "Thêm nhạc" đổi từ `<label for="audio-upload">` (trigger input ngay)
thành `<button id="btn-upload-audio">` mở **1 menu nhỏ 2 lựa chọn**
("Chọn file nhạc" / "Chọn cả thư mục") — cùng cơ chế định vị "fixed, JS đặt
lại vị trí ngay dưới nút bấm, lật lên nếu tràn đáy màn hình" như menu 3
chấm `#song-action-menu` đã có, dùng CHUNG overlay `#song-action-overlay`
để đóng khi bấm ra ngoài.

**Xử lý chung 100%, không nhánh code riêng:** logic xử lý `FileList` (đọc
tag, đọc cover, đọc duration, validate định dạng, ghi IndexedDB...) được
tách thành 1 hàm `handleAudioFiles(fileList)` duy nhất
(`js/playlist/loader.js`), cả 2 input (`audio-upload` và
`audio-upload-folder`) đều gọi hàm này khi `change`. File không phải nhạc
lẫn trong thư mục (ảnh cover rời, `.DS_Store`, file văn bản...) tự động bị
`validateAudioFile()` loại — y hệt cách input file rời loại file sai định
dạng cố tình chọn, không cần logic lọc riêng cho trường hợp thư mục.

## 5. Tách `js/components/settings-drawer.js` thành module nhiều file

**Vấn đề:** file này (chỉ chứa 1 biến `TPL_SETTINGS_DRAWER`) đã phình tới
~28KB / 266 dòng HTML dồn vào một template string — khó định vị, khó diff,
khó review khi chỉ sửa 1 mục cài đặt nhỏ trong số hàng chục mục.

**Giải pháp — tách theo đúng 5 khối `<h3>` (section) đã tồn tại sẵn trong
HTML gốc, không gộp/tách lại ranh giới:**

| File mới (`js/components/settings/`) | Biến TPL | Nội dung |
|---|---|---|
| `playlist-background.js` | `TPL_SETTINGS_PLAYLIST_BG` | Video Background, ảnh nền Playlist + độ mờ nhòe |
| `visualizer-geometry-color.js` | `TPL_SETTINGS_VISUALIZER` | Chất lượng render, độ cao/độ dày thanh, kiểu Vortex/Bar/Rain, màu nền + chế độ màu sóng âm |
| `audio-eq.js` | `TPL_SETTINGS_AUDIO_EQ` | Âm lượng tổng, preset Equalizer, dải tần số thủ công |
| `subtitle-style.js` | `TPL_SETTINGS_SUBTITLE_STYLE` | Toàn bộ style khung/chữ phụ đề (màu, độ trong suốt, viền, cỡ chữ, line-height, letter-spacing) |
| `misc.js` | `TPL_SETTINGS_MISC` | Giữ màn hình sáng, nút mở About Drawer |

`js/components/settings-drawer.js` KHÔNG còn chứa HTML trực tiếp — giờ là
1 object điều phối viết theo đúng dạng OBJECT-FUNCTION đã dùng ở
`PlaylistMain` (`js/playlist/main.js`):

```js
const SettingsDrawer = {
    renderHeader() { /* header + nút đóng */ },
    renderFooter() { /* đóng thẻ drawer */ },
    build() { /* nối renderHeader() + 5 biến TPL_SETTINGS_* + renderFooter() */ }
};
const TPL_SETTINGS_DRAWER = SettingsDrawer.build();
```

Biến `TPL_SETTINGS_DRAWER` được GIỮ NGUYÊN TÊN và vẫn là 1 string hoàn
chỉnh ngay khi file được nạp (gọi `SettingsDrawer.build()` đồng bộ ở cuối
file, KHÔNG đợi `DOMContentLoaded`) — `main.js` (bootstrap) không cần sửa
gì cả, vẫn ghép `TPL_SETTINGS_DRAWER` vào `#app-root` như mọi bản trước.

**Thứ tự nạp script — bắt buộc:** 5 file trong `js/components/settings/`
PHẢI nạp TRƯỚC `settings-drawer.js` (biến `TPL_SETTINGS_PLAYLIST_BG` v.v.
phải tồn tại trước khi `SettingsDrawer.build()` chạy). `index.html` đã cập
nhật đúng thứ tự này.

**Đã kiểm chứng giữ ĐÚNG 100% HTML output** so với bản gốc: ghép 6 file
mới lại rồi so sánh chuỗi `TPL_SETTINGS_DRAWER` sinh ra với bản trước khi
tách (sau khi chuẩn hóa khoảng trắng/indent — vốn thay đổi tự nhiên khi
chia file) cho kết quả GIỐNG NHAU TUYỆT ĐỐI; đối chiếu danh sách 58 `id`
HTML theo đúng thứ tự xuất hiện cũng khớp 100% — không mất, không lặp,
không đổi thứ tự bất kỳ phần tử nào mà `dom-refs.js`/`equalizer-settings.js`/
`player-controls.js` tham chiếu tới.

## 6. Gom 7 file `CHANGELOG_vX.md` vào thư mục `changelog/`

**Trước:** `CHANGELOG_v1.md` … `CHANGELOG_v7.md` nằm rải ở thư mục gốc,
cùng cấp với `README.md`, `index.html` — khiến thư mục gốc project ngày
càng nhiều file khi đánh số version tăng lên.

**Sau:** chuyển toàn bộ vào `changelog/`, đổi tên ngắn gọn còn `v1.md` …
`v8.md` (tiền tố `CHANGELOG_` đã thừa vì nằm trong thư mục `changelog/`).
`README.md` cập nhật toàn bộ đường dẫn liên kết tương ứng
(`./changelog/v7.md` thay cho `./CHANGELOG_v7.md`, …).

## 7. Logo SAV viết lại: bỏ khung/viền, hover trượt chữ theo chiều ngang (refine)

**Phản hồi:** logo lần đầu có khung/nền bo tròn, và hiệu ứng hover từng dự
định xếp 3 dòng dọc — không đúng ý muốn ban đầu (kiểu wordmark trần như
logo Facebook, trượt chữ theo chiều ngang, không xuống dòng).

**Sửa:** bỏ hẳn `bg-white/5`/`border`/`rounded-full` — chỉ còn chữ trắng
đậm (`font-extrabold text-white`). Mỗi chữ hoa (S/A/V) là 1 `<span>` đứng
yên; ngay sau nó là 1 `<span>` chứa phần chữ thường còn lại của từ ("imple
"/"udio "/"isualizer") với `max-width: 0` lúc nghỉ, giãn ra theo
`group-hover` LUÔN TRÊN 1 DÒNG NGANG (không xuống dòng) khi hover, nối
liền thành "Simple Audio Visualizer". `transition-delay` tăng dần
0/60/120ms cho cảm giác trượt nối tiếp từ trái qua phải. Bỏ hover thì
animate ngược lại co về "S A V" — cùng 1 transition cho 2 chiều.

## 8. Modal sửa thông tin/thông tin chi tiết: thiết kế lại theo `glass-modal` (refine)

**Phản hồi:** `.glass-panel` ban đầu áp cho 2 modal quá mờ nhạt, chữ/control
chưa nổi rõ trên ảnh nền Playlist sáng màu.

**Sửa:** thêm class `.glass-modal` riêng (nền đậm hơn, viền sáng hơn, có
glow nhẹ — xem mục 2 phần đầu changelog này để biết chi tiết đầy đủ). Thiết
kế lại toàn bộ nội dung: header có icon tròn màu, tab switcher kiểu "pill",
3 ô nhập có icon riêng, tab Ảnh bìa có nút icon+chữ rõ ràng, modal Thông tin
chi tiết chuyển từ list trần thành card từng dòng có icon màu riêng.

## 9. Thêm nhạc — hỗ trợ chọn cả thư mục (refine UI) + sửa lỗi treo khi upload (fix gốc rễ)

**Phản hồi lần 1:** nút "Thêm nhạc" đổi thành mở menu 2 lựa chọn ("Chọn file
nhạc" / "Chọn cả thư mục"), menu này dùng `<button>` gọi
`fileInput.click()`/`folderInput.click()` bằng JavaScript.

**Lỗi thực tế phát hiện sau đó:** chọn cả thư mục HOẶC từng file đều "treo",
không hiện cả loading shield. **Nguyên nhân gốc:** một số trình duyệt/
WebView (đặc biệt chạy qua `file://`, hoặc Android WebView cũ) CHẶN HOÀN
TOÀN việc `input[type=file]` phản hồi với `.click()` gọi gián tiếp bằng JS
— không lỗi, không event, im lặng chờ `change` không bao giờ tới. Chỉ click
NATIVE thật (chuột/chạm) lên `<label>`/`<input>` mới chắc chắn hoạt động
trên mọi nền tảng.

**Sửa:** 2 mục trong menu đổi từ `<button>` thành `<label>` BỌC TRỰC TIẾP
`<input type="file">` ẩn bên trong — đúng pattern đã dùng ổn định ở
`setting-bg-upload`/`setting-video-upload`. Bỏ hẳn `fileInput.click()`/
`folderInput.click()` trong JS. 2 input di chuyển từ `index.html` (ngoài
`#app-root`) vào hẳn trong template `playlist-view.js` (trong `#app-root`,
bên trong 2 `<label>` đó). Đóng menu sau khi bấm label dùng
`setTimeout(...,0)` (không đóng ngay trong cùng tick của sự kiện click) để
không cản trở browser kích hoạt input bên trong label.

## 10. Kiểm tra & sửa các trường hợp lỗi ảnh cover không hiển thị

**Vấn đề 1 — `<img>` "vỡ" khi Blob cover không decode được:** cover ID3 lỗi/
cắt cụt, hoặc người dùng upload ảnh hỏng qua tab "Ảnh bìa", khiến `<img
src="blob:...">` hiện icon ảnh hỏng của trình duyệt thay vì ảnh vinyl mặc
định.

**Sửa:** thêm hàm dùng chung `attachCoverFallback(imgEl)` (định nghĩa ở
`render.js`) — gắn `onerror` tự thay `src` về `DEFAULT_VINYL`, tự gỡ
listener sau 1 lần xử lý (tránh loop). Áp dụng cho MỌI nơi hiển thị cover:
danh sách bài hát (grid/list), `#record-art` ở player (tạo lại qua
`innerHTML` mỗi lần đổi bài — gắn lại fallback mỗi lần), và preview ảnh
trong modal sửa thông tin (`<img>` cố định, chỉ cần gắn 1 lần).

**Vấn đề 2 — nguồn gốc cover hỏng từ `jsmediatags`:** `tagResult.tags.
picture.format` đôi khi RỖNG hoặc KHÔNG PHẢI MIME ảnh hợp lệ (file MP3 ghi
ID3 không chuẩn) — `Blob` constructor không throw lỗi dù `type` rác, nhưng
`<img>` sau đó không decode được.

**Sửa:** validate `format` bằng `VALID_IMAGE_MIME_TYPES` (đã có ở
`upload-validation.js`) NGAY tại nguồn (`loader.js`, lúc đọc tag) — nếu sai
hoặc `data` rỗng, bỏ cover (`null`) thay vì lưu 1 Blob chắc chắn không hiển
thị được. Bài hát vẫn nạp bình thường, chỉ là không có ảnh bìa.

**Vấn đề 3 — `mediaSession.artwork` hard-code sai MIME:** `type:
'image/jpeg'` cố định dù cover thật có thể là PNG/WEBP — có thể khiến hệ
điều hành/màn hình khóa từ chối hiển thị artwork. Sửa dùng đúng
`record.cover.type` thật (fallback `'image/jpeg'` chỉ khi `type` rỗng).

**Vấn đề 4 — rò bộ nhớ object URL:** `buildSongNode()` tạo `URL.
createObjectURL()` mới mỗi lần gọi (mỗi lần render diff/refresh) mà không
bao giờ `revokeObjectURL()` URL cũ. Thêm `node._coverObjectUrl` theo dõi +
hàm `revokeNodeCoverUrl()`, gọi ở mọi nơi 1 node bị loại khỏi
`domNodesByKey` (`renderPlaylistFull`/`renderPlaylistDiff`/
`refreshSongNode`).

## 11. Hợp nhất Settings Visualizer/Phụ đề thành drawer riêng (giống About)

**Yêu cầu:** Settings chính quá dài (Chất lượng Render, hình học theo từng
kiểu visual, màu sắc, toàn bộ slider style phụ đề...). Hợp nhất thành
drawer riêng, chỉ giữ lại phần cần dùng ngay.

**Sửa — 2 drawer mới, đúng pattern navigation stack đã có ở About/Storage
Drawer** (`z-[90]`, `translate-y-full`, nút Back chỉ ẩn drawer con):

- **`drawer-visualizer-settings`** ("Tùy chỉnh Visualizer"): chứa Chất
  lượng Render + toàn bộ hình học theo từng kiểu (độ cao/độ dày thanh, kiểu
  Vortex/Bar/Rain...) + Màu sắc Visualizer. Settings chính giờ chỉ còn
  **select "Kiểu hiệu ứng"** (chọn trực tiếp 1 trong 6 visual — Bar/
  Lightning/Rubik/Vortex/Black Hole/Rain, thay cho việc bấm nút cycle nhiều
  lần) + nút mở drawer này.
- **`drawer-subtitle-settings`** ("Tùy chỉnh Phụ đề"): chứa toàn bộ slider
  màu/viền/cỡ chữ/line-height/letter-spacing. Settings chính chỉ còn toggle
  "Hiện phụ đề" + nút "Tùy chỉnh" mở drawer này.

Audio EQ (âm lượng + equalizer) GIỮ NGUYÊN không đổi, ngay trong Settings
chính, theo đúng yêu cầu. Select "Kiểu hiệu ứng" và nút cycle
(`#btn-cycle-mode`) dùng CHUNG 1 nguồn sự thật (`currentModeIndex` +
`updateTypeUI()`) — đổi từ đâu cũng đồng bộ 2 UI.

## 12. Control Center: phóng ra từ trung tâm + kính mờ trong suốt kiểu iPhone

**Phản hồi:** panel Control Center (mở từ nút góc trái màn Visualizer) lúc
mới làm trượt từ trên xuống full chiều rộng, nền `.drawer-glass` đậm — chưa
đúng cảm giác "phóng ra từ khung" + "kính mờ gần trong suốt" kiểu iPhone.

**Sửa:** đổi animation từ `-translate-y-full` (trượt) sang `scale-0
opacity-0` + `transform-origin: top left` (phóng ra từ đúng vị trí nút bấm,
giống Control Center iOS thật). Panel co lại thành 1 khối vuông góc bo tròn
kích thước theo nội dung (không còn full-width). Thêm class `.glass-
control-center` mới trong `css/styles.css` — nhạt hơn HẲN mọi class glass
khác trong app (`rgba(255,255,255,0.12)` + `blur(36px) saturate(1.6)`),
gần như chỉ làm mờ-đục cảnh phía sau thay vì che bằng 1 lớp nền đậm.

## 13. Loại bỏ "pause tạm khi đổi tab" — dừng hẳn + reset; bỏ điều khiển từ ngoài app

**Bối cảnh:** 1 lượt refine trước đã thêm hành vi "tab/app bị ẩn ->
tự pause nhạc, ẩn visual tạm, KHÔNG tự resume khi quay lại". Phản hồi yêu
cầu loại bỏ hẳn cách này, thay bằng dừng triệt để.

**Sửa — `resetPlayerToIdle()` (mới, `player-controls.js`):** dừng audio
(`pause()` + `currentTime = 0` + bỏ `src`), revoke object URL, đưa
`currentKey = null`, reset toàn bộ UI player về ĐÚNG trạng thái ban đầu
("Chưa chọn bài" / "---" / record-art rỗng / progress bar 0 / "0:00") —
playlist KHÔNG bị đụng (không xoá bài, không đổi `playlistOrder`/
`displayOrder`), chỉ là "chưa chọn bài nào" như mới mở app. `wakelock.js`
gọi hàm này khi `visibilitychange` báo tab/app bị ẩn (CHỈ nếu đang có bài
chọn — tránh gọi thừa); tab hiện lại KHÔNG còn làm gì tự động nữa (không
reset thêm lần nào, không resume). Cờ `isVisualForceHiddenByTab`/
`wasPlayingBeforeTabHidden` của bản trước bị XOÁ HẲN — không còn cần nữa vì
không có gì đang phát để mà ẩn tạm.

**Bỏ hẳn `navigator.mediaSession.setActionHandler(...)`** (`player-
controls.js`) — play/pause/next/prev/seek từ màn hình khoá, tai nghe, hoặc
nút điều khiển trên thông báo hệ thống KHÔNG còn tác dụng gì cả.
`navigator.mediaSession.metadata` (tên bài/ảnh hiển thị trên thông báo) và
`.playbackState` (trạng thái playing/paused hiển thị) VẪN GIỮ NGUYÊN — đây
chỉ là thông tin hiển thị một chiều, không phải đường điều khiển ngược lại
vào app, nên không thuộc phạm vi "ngăn control can thiệp".

---

### Tổng kết file thay đổi/thêm mới ver 8

- **Mới:**
  `js/components/settings/playlist-background.js`,
  `js/components/settings/visualizer-geometry-color.js`,
  `js/components/settings/audio-eq.js`,
  `js/components/settings/subtitle-style.js`,
  `js/components/settings/misc.js`,
  `js/components/visualizer-settings-drawer.js`,
  `js/components/subtitle-settings-drawer.js`,
  `changelog/v8.md` (file này).
- **Sửa:** `js/components/playlist-view.js` (logo SAV wordmark trượt ngang,
  tab Ảnh bìa + thiết kế lại 2 modal theo `glass-modal`, menu "Chọn file/
  Chọn cả thư mục" dùng label bọc input), `js/components/visualizer-
  overlay.js` (Control Center: grid icon thay dải dọc, phóng từ trung tâm +
  kính mờ trong suốt), `js/components/subtitle-modal.js` (bỏ nút Bật/Tắt
  Sub, chuyển về Settings), `js/components/settings-drawer.js` (viết lại
  hoàn toàn thành object điều phối), `js/main.js` (ghép 2 TPL drawer mới),
  `js/core/dom-refs.js` (nhiều ref mới: `savLogo`, `folderInput`,
  `btnUploadAudio`, `uploadActionMenu`, Control Center, 2 drawer Settings
  mới...), `js/core/equalizer-settings.js` (toggle phụ đề lưu config, logic
  mở/đóng 2 drawer mới, select Kiểu hiệu ứng), `js/core/player-controls.js`
  (gọi `requestWakeLock()` đầu `playNext`/`playPrev`, hàm
  `resetPlayerToIdle()` mới, bỏ `setActionHandler`, đồng bộ select Kiểu
  hiệu ứng), `js/core/wakelock.js` (viết lại `visibilitychange`: dừng hẳn +
  reset thay vì pause tạm), `js/core/state-and-video-bg.js` (mở/đóng
  Control Center, bỏ ràng buộc Tắt Visual khỏi video nền), `js/core/
  subtitle-display.js`, `js/core/subtitles.js` (đồng bộ toggle phụ đề mới),
  `js/core/config.js` (`subtitlesEnabled`, `visualEnabled` mới),
  `js/visualizers/draw-visualizer.js` (Tắt Visual độc lập khỏi video nền),
  `js/playlist/actions.js` (logic tab Ảnh bìa, `openSongInfoModal` dạng
  card, cover fallback, sửa MIME mediaSession), `js/playlist/loader.js`
  (`handleAudioFiles()` dùng chung, validate MIME cover từ jsmediatags),
  `js/playlist/order.js` (`matchesSearch` lọc thêm theo album),
  `js/playlist/render.js` (`attachCoverFallback`/`revokeNodeCoverUrl`),
  `css/styles.css` (class `.glass-modal`, `.glass-control-center` mới),
  `index.html` (thứ tự nạp script mới, bump version `?v=` nhiều lần),
  `README.md`.
- **Di chuyển + đổi tên:** `CHANGELOG_v1.md` … `CHANGELOG_v7.md` →
  `changelog/v1.md` … `changelog/v7.md`.
